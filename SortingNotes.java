/**
 * Sorting Algorithm Complexity Reference
 *
 * | Algorithm    | Best     | Average  | Worst    | Space  |
 * |--------------|----------|----------|----------|--------|
 * | Bubble Sort  | O(n)     | O(n²)    | O(n²)    | O(1)   |
 * | Count Sort   | O(n+k)   | O(n+k)   | O(n+k)   | O(k)   |
 * | Binary Search| O(1)     | O(log n) | O(log n) | O(1)   |
 *
 * Notes:
 * - Bubble sort is stable but inefficient for large datasets
 * - Count sort excels when range of values (k) is small
 * - Always prefer binary search over linear search on sorted data
 */
public class SortingNotes {
    // This class serves as a reference document for algorithm analysis
}
